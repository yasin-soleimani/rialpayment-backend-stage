<!DOCTYPE html>
<html dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>ریال پیمنت</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="https://core-backend.rialpayment.ir/assets/callback/style.css" />
</head>
<body>
    <div class="container">
        <div class="card">
            <div class="logo">
                <img src="https://core-backend.rialpayment.ir/assets/callback/assets/logo600.png" width="200" alt="">
            </div>
            <div class="card-body">
                <div class="title">
                    <p>
                        <%= info.mobile %>، پرداخت شما
                        <% if (status == 'ok') { %>
                        <span style="color: limegreen;">با موفقیت انجام شد
                        <% } %>
                        <% if (status == 'nok') { %>
                            <span style="color: red;">با  مشکل مواجه شده است</span>
                        <% } %>
                    </p>
                </div><br>
                <div class="subtitle">
                    <div style="display: inline-flex">
                        <p>شماره تراکنش </p>
                        <p>:</p>
                        <p><%= info.invoiceid %></p>
                    </div>
                    <div style="display: inline-flex">
                        <p>مبلغ</p>
                        <p>:</p>
                        <p><strong style="color: #000;"><%= info.amount %> ریال</strong></p>
                    </div>
                </div><br>
                <div class="emoji">
                    <div>
                            <% if (status == 'ok') { %>
                        <img src="https://core-backend.rialpayment.ir/assets/callback/assets/smiling.svg" width="96">
                        <% } %>
                        <% if (status == 'nok') { %>
                            <img src="https://core-backend.rialpayment.ir/assets/callback/assets/sad.svg" width="96">
                        <% } %>
                    </div>
                </div><br><br>
                <div class="card-footer">
                    <p>جهت تکمیل فرآیند خرید کلیک کنید</p>
                    <% if (status == 'ok') { %>
                        <a class="btn" href="http://ticket.sina.rialpayment.ir/ticket/<%= info.userinvoice %>">تکمیل خرید</a>
                    <% } else {%>
                            <a class="btn" href="http://eram.rialpayment.ir">بازگشت</a>
                    <% } %>

                    </div>
            </div>
        </div>
    </div>
</body>
</html>
