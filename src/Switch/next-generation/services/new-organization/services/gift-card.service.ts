import { Injectable } from '@vision/common';

Injectable();
export class NewOrganizationSwitchGiftCardService {
  constructor() {}

  async payment(getInfo, cardInfo, terminalInfo, discountInfo): Promise<any> {}
}
